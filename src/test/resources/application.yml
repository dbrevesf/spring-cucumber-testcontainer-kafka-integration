spring:
  kafka:
    container-image: confluentinc/cp-kafka:6.2.1
    consumer:
      auto-offset-reset: earliest
      group-id: tw-exxon-test
    producer:
      bootstrap-servers: localhost:9092
test:
  topic: embedded-test-topic